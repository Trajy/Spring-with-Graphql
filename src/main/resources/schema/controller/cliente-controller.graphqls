extend type Query {
    clienteController: ClienteController!
}

extend type Mutation {
    clienteController: ClienteController!
}

type ClienteController {
    cliente: Cliente
    findById(id: ID!): Cliente
    save(cliente: Cliente!): ID!
    update(cliente: Cliente!): Cliente
    delete(id: ID!): Boolean
}