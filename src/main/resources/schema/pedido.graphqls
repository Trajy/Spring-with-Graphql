extend type Query {
    pedido(id: ID): Pedido
}

type Pedido {
    id: ID
    codigo: String
    cliente: Cliente
    produtos: [Produto]
}

input PedidoInput {
    id: ID
    codigo: String
    cliente: ClienteInput!
    produtos: [ProdutoInput]
}