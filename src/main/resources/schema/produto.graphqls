extend type Query {
    produto(id: ID): Produto
}

type Produto {
    id: ID
    nome: String
    preco: Float
    pedidos: [Pedido]
}

input ProdutoInput {
    id: ID
    nome: String
    preco: Float
    pedidos: [PedidoInput]
}